services:
  eeg_seizure:
    build:
      context: .
      dockerfile: Dockerfile
    command: tail -f /dev/null # This keeps the container running
    stdin_open: true
    tty: true
    volumes:
      - .:/app # Mount the code
      # Please change the data path to your own path
      - /Users/fchen2/dev/team_ai/jiangjun/Data:/app/benchmark/Data:ro # Mount data as read-only
